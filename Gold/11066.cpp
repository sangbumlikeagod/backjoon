#include <iostream>
#include <fstream>
#include <vector>
#include <queue>
using namespace std;

int ptsum(int s, int e, ){
    if (e - s == 1){
        return 
    }

}


int main(){
    ifstream cin("11066.txt", ios_base::in);
    ios::sync_with_stdio(false), cin.tie(0), cin.tie(0);
    int T;
    cin >> T;
    // cout << T;
    for (int i = 0; i < T; i++){
        int* arr[501][501];
        // cout << arr[0][0];

        int total = 0;

        cout << total << '\n';
    }


}   