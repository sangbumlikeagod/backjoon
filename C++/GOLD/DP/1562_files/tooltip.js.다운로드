$(document).ready(function() {
    $("[rel=tooltip]").tooltip();
    $("[rel=tooltip]").click(function() {
        if ($(this).hasClass('tooltip-click')) {
            return true;
        } else {
            return false;
        }
    });
});
